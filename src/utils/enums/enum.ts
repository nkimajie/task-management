export enum activeInactiveStatus {
  Active = 'Active',
  Inactive = 'Inactive',
}

export enum returnRequestStatus {
  Pending = 'Pending',
  Confirmed = 'Confirmed',
  Denied = 'Denied',
}

export enum userTypeEnum {
  user = 'USER',
  admin = 'ADMIN',
}

export enum productEnum {
  published = 'PUBLISHED',
  lowStock = 'LOW STOCK',
  outOfStock = 'OUT OF STOCK',
  draft = 'DRAFT',
}

export enum emailTemplete {
  PASSWORD_RESET_CONFIRM = 'email-password-reset-successful',
}
